/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  // Bất kỳ cấu hình Next.js nào khác của bạn có thể ở đây
};
 
export default nextConfig; 